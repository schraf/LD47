
enum Team {
  Neutral;
  A;
  B;
}

enum PlayerRole {
  Jammer;
  Blocker;
}

enum TrackDirection {
  Invalid;
  Down;
  Left;
  Right;
  Up;
}

enum ItemType {
  SpeedBoost;
  Teleport;
  Explosion;
  Invisibility;
  SpeedTrap;
  Freeze;
  Shuffle;
}
